# Textos prontos para substituição no Capítulo 4

> **Versão base:** `TCC Gabriel Steffens Atualizado 20_09.docx.md`
>
> **Como usar.** Cada item traz o bloco completo, do título da subseção até o seu encerramento,
> incluindo os parágrafos originais preservados, as chamadas de figura e os blocos de código já
> posicionados. Onde a ação é substituir, basta apagar a subseção no documento e colar o bloco
> inteiro. Onde a ação é acrescentar, o item indica a frase exata após a qual o texto entra.
>
> **Código citado.** Conferido contra o repositório em 22/09.

## Índice

| Item | Subseção | Ação | Linha no documento |
|---|---|---|---|
| 1 | 4.4.2 Camada de Domínio | Acrescentar um parágrafo | dentro do bloco da linha 660 |
| 2 | 4.4.3 Camada de Dados | Acrescentar um parágrafo | dentro do bloco da linha 660 |
| 3 | 4.4.6 Sincronização e Consistência Eventual | Substituir o bloco inteiro | 662 a 670 |
| 4 | 4.4.8 Garantia de Entrega do Alarme | Substituir o bloco inteiro | 702 a 706 |
| 5 | 4.4.9 Limitações Técnicas Identificadas | Incluir bloco novo | após o item 4 |
| 6 | 4.6.2 Cadastro de medicamentos | Acrescentar um parágrafo | dentro do bloco da linha 891 |

Ao todo, onze parágrafos novos e três blocos de código.

**Sobre a 4.4.7.** Não sofre alteração. Os dois trechos de código citados nela foram
conferidos e permanecem idênticos ao código em produção.

**Atenção.** No documento, as subseções 4.4.1 a 4.4.5 estão reunidas em um único parágrafo
corrido, na linha 660. Os itens 1 e 2 indicam a frase exata após a qual inserir.

---

# Item 1 — Subseção 4.4.2, um parágrafo para acréscimo

**Onde inserir.** No bloco da linha 660, localizar o fim da subseção 4.4.2, que termina com a
frase abaixo.

> *"Essa decisão torna os cálculos determinísticos e verificáveis, permitindo simular a virada
> da meia-noite ou uma dose em atraso sem depender do relógio do dispositivo de teste."*

**Inserir logo após essa frase, antes de "4.4.3 Camada de Dados", o texto a seguir.**

---

&nbsp;&nbsp;&nbsp;&nbsp;Oito rotinas de conferência exercitam os casos de uso diretamente em ambiente Node, sem emulador e sem interface gráfica, cobrindo o cálculo de adesão diária, a previsão de esgotamento de estoque, o planejamento de avisos de dose e de compromisso e o encadeamento de edições em prescrições. A execução fora do aparelho só é possível porque esses casos de uso não importam banco de dados nem biblioteca de notificações, conforme a regra de dependência apresentada na seção 4.4.1.

---

# Item 2 — Subseção 4.4.3, um parágrafo para acréscimo

**Onde inserir.** Ainda no bloco da linha 660, localizar na subseção 4.4.3 a frase abaixo.

> *"Essa restrição decorre da necessidade de preservar o histórico clínico de instalações já em
> uso, para as quais uma migração destrutiva representaria perda de dados de tratamento."*

**Inserir logo após essa frase o texto a seguir.**

---

&nbsp;&nbsp;&nbsp;&nbsp;A base acumula vinte migrações, aplicadas em ordem crescente na inicialização da aplicação, e a versão corrente do esquema fica registrada na própria base. Cada migração é executada em transação própria, e a versão é marcada somente após a confirmação da transação, aplicando a atomicidade discutida na seção 2.9.1.

---

# Item 3 — Subseção 4.4.6, bloco completo para substituição

**Onde.** Linhas 662 a 670. Apagar desde `**4.4.6 Sincronização e Consistência Eventual**` até
o fim do parágrafo sobre segurança em nível de linha, e colar o bloco abaixo por inteiro.

---

**4.4.6 Sincronização e Consistência Eventual**

&nbsp;&nbsp;&nbsp;&nbsp;A sincronização entre a base local e a base remota segue o modelo de Last-Write-Wins apresentado na seção 2.9.3, no qual o registro com o carimbo de tempo mais recente prevalece integralmente sobre a versão anterior, sem mesclagem de campos individuais. Se o sistema tentasse combinar campos de duas versões diferentes de um mesmo registro, poderia produzir um estado que nenhum dos dois dispositivos realmente gerou, misturando, por exemplo, a dose de uma edição com o horário de outra.

Figura 1 \- Diagrama de sequência da resolução de conflito de sincronização entre dispositivo local e servidor em nuvem pelo critério *Last-Write-Wins*.
![][image1]
Fonte: elaborado pelo autor.

&nbsp;&nbsp;&nbsp;&nbsp;A aplicação do critério ocorre no momento da recepção, comparando o carimbo de tempo da versão remota com o da versão local antes de qualquer escrita, conforme o trecho a seguir.

1. **const** local \= **await** database.**getFirstAsync**\<{ updated\_at: string }\>(
2. &nbsp;&nbsp;\`SELECT updated\_at FROM ${tabela} WHERE id \= ?\`,
3. &nbsp;&nbsp;\[id\],
4. );
5.
6. *// LWW por \`updated\_at\`, tudo-ou-nada por registro: local igual ou mais novo vence. Sem merge*
7. *// de campos, que poderia produzir uma posologia que ninguém escreveu. O empate fica com o local*
8. *// porque empate só acontece quando os dois lados já têm a mesma coisa.*
9. **if** (local \!== **null** && local.updated\_at \>= remotaUpdatedAt) {
10. &nbsp;&nbsp;*// ...*
11. &nbsp;&nbsp;**continue**;
12. }

&nbsp;&nbsp;&nbsp;&nbsp;O empate favorece a versão local. O envio seleciona as linhas cujo carimbo de modificação é posterior ao de sincronização, e grava esse último somente após a confirmação do servidor, o que mantém pendente para o ciclo seguinte qualquer edição feita durante o envio. As linhas marcadas para exclusão lógica também sobem, de modo que a remoção alcance os demais aparelhos.

&nbsp;&nbsp;&nbsp;&nbsp;A transferência ocorre em lotes de duzentas linhas, e cada tabela mantém um marcador com o instante da última recepção, que restringe a consulta seguinte ao que mudou desde então. O marcador reside em tabela da base local, sendo descartado junto com os dados na exclusão total solicitada pelo paciente.

&nbsp;&nbsp;&nbsp;&nbsp;No nível de segurança, a comunicação entre a aplicação e a base remota é protegida por políticas de segurança em nível de linha, aplicadas às nove tabelas sincronizáveis, restringindo toda operação de leitura, inserção e atualização ao proprietário do registro, identificado pelo token de autenticação do usuário. Essa redundância é deliberada. Ainda que a política de segurança em nível de linha já seja suficiente para garantir o isolamento dos dados, a aplicação repete o filtro por identificador de usuário como uma segunda camada independente de verificação, de modo que as duas camadas precisam concordar entre si para que uma operação seja concluída.

---

# Item 4 — Subseção 4.4.8, bloco completo para substituição

**Onde.** Linhas 702 a 706. Apagar desde `**4.4.8 Garantia de Entrega do Alarme**` até o fim
do terceiro parágrafo, imediatamente antes de `### 4.5 MODELO DE DADOS`, e colar o bloco
abaixo por inteiro.

---

**4.4.8 Garantia de Entrega do Alarme**

&nbsp;&nbsp;&nbsp;&nbsp;A biblioteca inicialmente adotada para o agendamento de alarmes, Notifee, teve sua manutenção descontinuada, e seu mecanismo de reinicialização automática após a reinicialização do aparelho (*boot receiver*) deixou de ser corretamente invocado a partir do Android 12. Como o disparo confiável do alarme mesmo após o desligamento do aparelho é um requisito central deste projeto, a aplicação migrou para o *fork* mantido da biblioteca, react-native-notify-kit, que preserva a interface de programação original.

&nbsp;&nbsp;&nbsp;&nbsp;Essa biblioteca é a base do mecanismo de disparo do alarme de dose. Ela delega o agendamento ao gerenciador nativo de alarmes do sistema operacional. Essa escolha é central para o requisito de confiabilidade discutido na seção 4.2.2. Ao transferir a responsabilidade de guardar o horário para o próprio sistema operacional, e não para um processo da aplicação, o alarme dispara mesmo com o aplicativo encerrado e sem nenhum processo em execução na memória do aparelho, condição indispensável diante do contexto apresentado. Os botões de ação da notificação são construídos diretamente no momento do agendamento, o que mantém a aplicação livre de qualquer indireção entre o que é oferecido ao paciente e o que a notificação de fato executa.

&nbsp;&nbsp;&nbsp;&nbsp;A entrega do alarme contempla dois pontos de acionamento distintos, sendo o primeiro uma tela nativa, exibida em primeiro plano sobre a tela de bloqueio quando a aplicação está em segundo plano, e o segundo uma rota interna da aplicação, utilizada quando o alarme dispara com a aplicação já em uso. Como os dois pontos de acionamento compartilham o mesmo processo de execução, a aplicação mantém um controle de estado em escopo de módulo, e não vinculado à árvore de componentes da interface, garantindo que apenas um dos dois caminhos efetivamente conduza à resposta do paciente. A consulta que decide qual caminho prevalece considera o custo de cada erro possível, e não apenas a informação disponível no momento.

1. **export** **function** **jaEstaEmCena**(scheduledFor: string): boolean {
2. &nbsp;&nbsp;**if** (activityNascendo && \!activityConhecida) **return** **true**;
3. &nbsp;&nbsp;**return** emCena.**has**(scheduledFor);
4. }

&nbsp;&nbsp;&nbsp;&nbsp;Enquanto a tela nativa é construída, a aplicação ainda não sabe a qual horário ela corresponde, e nessa janela a consulta responde afirmativamente por precaução. O critério considera a assimetria entre os dois erros possíveis. Responder afirmativamente sem tela alguma custa um toque ignorado, ao passo que responder negativamente com a tela a caminho produz duas telas para a mesma dose, em que a resposta dada numa delas não encerra a outra, comprometendo a integridade do registro clínico discutida na seção 4.3.1.

&nbsp;&nbsp;&nbsp;&nbsp;O encerramento da tela também exigiu tratamento específico no nível nativo, por meio de um módulo próprio escrito em Kotlin. A tela do alarme ocupa uma atividade própria do Android, executada em tarefa separada, de modo que o encerramento convencional oferecido pela biblioteca de interface finalizaria o processo inteiro da aplicação, derrubando junto o serviço responsável pela reprodução do som.

1. **AsyncFunction**("fecharTelaDoAlarme") {
2. &nbsp;&nbsp;**val** activity \= appContext.currentActivity ?: **return**@AsyncFunction **false**
3. &nbsp;&nbsp;activity.**runOnUiThread** { activity.**finishAndRemoveTask**() }
4. &nbsp;&nbsp;**return**@AsyncFunction **true**
5. }

&nbsp;&nbsp;&nbsp;&nbsp;O módulo encerra apenas a tarefa do alarme e devolve o aparelho ao estado anterior ao disparo, que é a tela de bloqueio quando o alarme foi acionado com o aparelho bloqueado, de modo que a confirmação de uma dose não expõe os dados clínicos. A reprodução do som é conduzida pela própria aplicação em serviço de primeiro plano, e não pelo canal de notificação, conforme detalhado na seção 4.4.9.

---

# Item 5 — Subseção 4.4.9, bloco novo para inclusão

**Onde.** Inserir imediatamente após o fim do Item 4, antes da linha `### 4.5 MODELO DE DADOS`.

---

**4.4.9 Limitações Técnicas Identificadas**

&nbsp;&nbsp;&nbsp;&nbsp;O desenvolvimento do alarme descrito na seção 4.4.8 esbarrou em duas limitações da plataforma, registradas aqui por delimitarem o alcance da solução.

&nbsp;&nbsp;&nbsp;&nbsp;A primeira envolve o volume do alarme. A hipótese inicial atribuía o problema ao atributo de áudio do canal de notificação, que declarava finalidade de notificação comum em vez de despertador, e a aplicação passou a alterá-lo por complemento de compilação. A modificação foi aplicada corretamente ao código nativo, e ainda assim o alarme continuou sendo reproduzido no volume de mídia em duas compilações distintas. O som da notificação é reproduzido pelo gerenciador do sistema operacional, para o qual esse atributo constitui sugestão e não determinação, o que consta do repositório da biblioteca, onde a solicitação correspondente foi encerrada sem previsão de implementação.

&nbsp;&nbsp;&nbsp;&nbsp;A segunda envolve o restabelecimento do bloqueio de tela. Quando a atividade do alarme é exibida sobre a tela de bloqueio, o sistema operacional o dispensa para aquela atividade, e encerrar a tarefa depois não reverte a dispensa, uma vez que o Android não oferece interface para que uma aplicação rebloqueie o aparelho.

---

# Item 6 — Subseção 4.6.2, um parágrafo para acréscimo

**Onde inserir.** No bloco da linha 891, localizar o fim do primeiro parágrafo, que termina com
a frase abaixo.

> *"o que evita transferir ao reconhecimento automático a responsabilidade por um dado
> clínico."*

**Inserir logo após essa frase, antes do parágrafo que começa com "No formulário propriamente
dito", o texto a seguir.**

---

&nbsp;&nbsp;&nbsp;&nbsp;O catálogo deriva da base pública da Câmara de Regulação do Mercado de Medicamentos e reúne cerca de sete mil apresentações comerciais, com nome, princípio ativo, dosagem, exigência de receita e códigos de barras. O arquivo é embarcado na aplicação e importado para a base local uma única vez por instalação, o que mantém a consulta disponível no uso offline descrito na seção 4.1.

---

# Validação do código citado na seção 4.4.7

Conferidos contra o repositório em 22/09, ambos os trechos citados na 4.4.7 permanecem
idênticos ao código em produção, e a subseção não exige ajuste.

O tipo `IntakeLog`, em `src/domain/entities/intake-log.ts`, mantém os cinco campos na mesma
ordem apresentada no artigo. O método `execute` do caso de uso `CorrectIntake`, em
`src/domain/use-cases/correct-intake.ts`, mantém a constante `correctedLog` com os mesmos oito
campos e a mesma chamada ao repositório. A refatoração recente não alterou a camada de domínio
nesses pontos.

---

# Notas finais

**Recuo.** Os `&nbsp;` simulam a tabulação do documento. No Word, substituir pelo recuo do
estilo de parágrafo.

**Blocos de código.** Seguem o formato já estabelecido na 4.4.7, com numeração e negrito nas
palavras reservadas da linguagem. O bloco da 4.4.6 inclui duas linhas de comentário, que no
documento devem aparecer em itálico.

**Figuras.** A Figura 1 permanece na 4.4.6, na mesma posição em que já estava, e nenhuma
figura nova foi introduzida. Caso deseje ilustrar a 4.4.8, um diagrama de sequência entre os
dois pontos de acionamento seria o mais útil, e exigiria renumerar as figuras seguintes.

**Remissões a conferir.** O parágrafo da 4.4.3 remete à atomicidade discutida na
seção 2.9.1, e o da 4.4.2 remete à regra de dependência da 4.4.1. Convém conferir se essas
duas seções do referencial teórico sustentam a remissão na forma como está redigida, uma vez
que o texto novo pressupõe que ambas tratem dos conceitos citados.

**Numeração.** A inclusão da 4.4.9 desloca a numeração caso o documento venha a
receber outra subseção ao final da 4.4. As remissões internas entre a 4.4.8 e a 4.4.9 são
recíprocas, e precisam ser revistas juntas em caso de renumeração.
