# Modelo do .env (que não é commitado). Valores em Supabase → Settings → Data API e API Keys.
# A chave é a anon/publishable, nunca a service_role: EXPO_PUBLIC_ vai embutido no bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
